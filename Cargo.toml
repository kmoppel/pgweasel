[package]
name = "pgweasel-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.47", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
regex = "1.0"
once_cell = "1.21.3"
log = "0.4"
env_logger = "0.11"

[dev-dependencies]
tempfile = "3.0"
